// import 'package:closing/src/models/closing_stock.dart';
import 'package:test/test.dart';

// import '../dummy/closing_stock.dart';

Future main() async {
  group('closing stocks json', () {
    test('', () async {
      // final css = ClosingStock.fromJson(closingStocks);
      // closingStocks.map((e) => ClosingStock.fromJson(e)).toList();
    });
  });
}
